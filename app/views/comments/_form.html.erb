<%= form_with model: [lake, lake.comments.build ] do |form| %>
  <% if form.object.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(form.object.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% form.object.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div>
    <%= form.label :body, "Your Comment", style: "display: block" %>
    <%= form.text_area :body %>
  </div>

 

  <div>
    <%= form.submit %>
  </div>
<% end %>
